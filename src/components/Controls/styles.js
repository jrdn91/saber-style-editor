import { makeStyles } from "@material-ui/core/styles"

const styles = makeStyles((theme) => ({
  controls: {
    boxShadow:
      "0px 3px 3px -2px rgb(0 0 0 / 5%), 0px 3px 4px 0px rgb(0 0 0 / 3.5%), 0px 1px 8px 0px rgb(0 0 0 / 3%)",
    position: "relative",
    zIndex: 2,
  },
}))

export default styles
